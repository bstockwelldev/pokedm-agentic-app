# Copy this file to `.env` and replace the placeholder values with your keys.

# The model to use by default when the client doesn't specify one.
# Gemini models: gemini-2.5-flash, gemini-1.5-pro, gemini-1.5-flash, gemini-pro
LLM_MODEL=gemini-2.5-flash

# Google Gemini API key. Get one from https://aistudio.google.com/app/apikey
# Note: @ai-sdk/google v3+ uses GOOGLE_GENERATIVE_AI_API_KEY
GOOGLE_GENERATIVE_AI_API_KEY=your-gemini-api-key-here
GEMINI_API_KEY=your-gemini-api-key-here

# Groq API key. Get one from https://console.groq.com/keys
# Optional: If not set, fallback Groq models will still be available
GROQ_API_KEY=your-groq-api-key-here

# Optional: change the port for the Express server
PORT=3001

# Storage Provider: 'file' for local dev, 'postgres' for production
# Defaults to 'file' if not set
STORAGE_PROVIDER=file

# PostgreSQL Database URL (required if STORAGE_PROVIDER=postgres)
# Format: postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://user:password@localhost:5432/pokedm

# Database SSL (set to 'true' for production databases with SSL)
# DATABASE_SSL=false

# Sessions directory for JSON file storage (only used if STORAGE_PROVIDER=file)
SESSIONS_DIR=./sessions